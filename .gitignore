# --- Python ---
venv/
.venv/
__pycache__/
*.py[cod]

# --- Derived artifacts ---
# skills.db is rebuilt from data/*.jsonl by pipeline.py in seconds.
# The JSONL files ARE committed: reproducibility is part of the pitch.
*.db
*.sqlite3

# Raw scrape/fetch cache (see scraper.py TODO). Can get large; keep it local.
data/raw/

# --- Third-party job description text: HELD BACK WHILE THE REPO IS PUBLIC ---
# These contain ~674 KB of verbatim job descriptions belonging to Postman,
# Meesho and CRED. Committing them to a PUBLIC repo is redistribution of
# someone else's copyrighted content, which is a different act from reading a
# public API for research.
#
# Reproducibility is NOT lost: `python ingest_ats.py` regenerates both files
# from the public endpoints listed in data/boards.json in about ten seconds.
#
# Once the repo is private, delete these two lines and commit them.
data/postings_real.jsonl
data/recall_review.md

# --- Secrets / local config ---
.claude.json
.env

# --- OS ---
.DS_Store
Thumbs.db
